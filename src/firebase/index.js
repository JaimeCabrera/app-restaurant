import {
  firebase,
  db,
  storage,
  doc,
  getDocs,
  onSnapshot,
  collection,
  query,
  where,
} from './firebase';

export {
  firebase,
  db,
  storage,
  doc,
  getDocs,
  onSnapshot,
  collection,
  query,
  where,
};
